import {
  AnimatePresence,
  LazyMotion,
  domAnimation,
  m,
  useAnimation,
  useInView,
  useScroll,
  useSpring,
  useTransform,
  useMotionTemplate,
} from 'framer-motion';

export const motion = m;
export {
  AnimatePresence,
  LazyMotion,
  domAnimation,
  useAnimation,
  useInView,
  useScroll,
  useSpring,
  useTransform,
  useMotionTemplate,
};
